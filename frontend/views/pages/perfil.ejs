<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.landing.css">
  <link rel="stylesheet" href="/css/perfil.css">
  <title>Perfil - JESSKA</title>
</head>
<body>
  <header>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid">
        <h3><strong>JESSKA</strong></h3>
        <div class="collapse navbar-collapse show">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link" href="/">Inicio</a>
            </li>
          </ul>
          <div class="d-flex align-items-center gap-2">
            <span class="me-2">Hola, <strong><%= usuario?.nombre || usuario?.email %></strong></span>
            <form method="post" action="/logout" class="m-0">
              <button type="submit" class="btn btn-outline-danger btn-sm">Cerrar sesión</button>
            </form>
          </div>
        </div>
      </div>
    </nav>
  </header>

  <main class="perfil-wrapper d-flex">
    <aside class="perfil-sidebar p-3 border-end">
      <div class="mb-4">
        <h5 class="mb-0">Tu Panel</h5>
        <small class="text-muted">Bienvenido, <%= usuario?.nombre || usuario?.email %></small>
      </div>
      <nav class="nav flex-column gap-2" id="perfilMenu">
        <a href="#" data-section="home" class="nav-link active">Inicio</a>
        <a href="#" data-section="info" class="nav-link">Cambiar información</a>
        <a href="#" data-section="pedidos" class="nav-link">Tus pedidos</a>
        <a href="#" data-section="mensajes" class="nav-link">Mensajes</a>
        <a href="#" data-section="resenas" class="nav-link">Reseñas</a>
        <form method="post" action="/logout" class="mt-3">
          <button type="submit" class="btn btn-outline-danger w-100">Cerrar sesión</button>
        </form>
      </nav>
    </aside>

    <section class="perfil-content flex-grow-1 p-4">
      <% if (typeof success !== 'undefined' && success) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= success %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= error %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>

      <!-- Sección Inicio -->
      <div id="section-home" class="section">
        <div class="card shadow-sm">
          <div class="card-body">
            <h4 class="mb-2">¡Bienvenido a tu perfil, <%= usuario?.nombre || usuario?.email %>!</h4>
            <p class="text-muted">Aquí puedes actualizar tu información, revisar tus pedidos, leer tus mensajes y ver tus reseñas. ¡Gracias por ser parte de JESSKA!</p>
          </div>
        </div>
      </div>

      <!-- Sección Cambiar información -->
      <div id="section-info" class="section d-none">
        <div class="card shadow-sm">
          <div class="card-body">
            <h4 class="mb-3">Actualizar información</h4>
            <form method="post" action="/perfil" id="perfilForm" novalidate>
              <div class="mb-3">
                <label class="form-label">Nombre</label>
                <input type="text" name="nombre" class="form-control <%= (errorField==='nombre') ? 'is-invalid' : '' %>" value="<%= usuario?.nombre || '' %>">
                <div class="invalid-feedback"><%= errorField==='nombre' ? error : 'Ingrese su nombre (mínimo 3 caracteres).' %></div>
              </div>
              <div class="mb-3">
                <label class="form-label">Teléfono</label>
                <input type="text" name="telefono" class="form-control <%= (errorField==='telefono') ? 'is-invalid' : '' %>" value="<%= usuario?.telefono || '' %>">
                <div class="invalid-feedback"><%= errorField==='telefono' ? error : 'Ingrese un teléfono válido.' %></div>
              </div>
              <hr>
              <h6>Cambiar contraseña</h6>
              <div class="mb-3">
                <label class="form-label">Nueva contraseña</label>
                <input type="password" name="password" class="form-control <%= (errorField==='password') ? 'is-invalid' : '' %>" minlength="8">
                <div class="invalid-feedback"><%= errorField==='password' ? error : 'La contraseña debe tener al menos 8 caracteres.' %></div>
              </div>
              <div class="d-grid">
                <button type="submit" class="btn btn-primary">Guardar cambios</button>
              </div>
            </form>
          </div>
        </div>
      </div>

      <!-- Sección Pedidos -->
      <div id="section-pedidos" class="section d-none">
        <div class="card shadow-sm">
          <div class="card-body">
            <h4 class="mb-3">Tus pedidos</h4>
            <div id="pedidosList" class="vstack gap-3"></div>
          </div>
        </div>
      </div>

      <!-- Sección Mensajes -->
      <div id="section-mensajes" class="section d-none">
        <div class="card shadow-sm">
          <div class="card-body">
            <h4 class="mb-3">Tus mensajes</h4>
            <div id="mensajesList" class="vstack gap-3"></div>
          </div>
        </div>
      </div>

      <!-- Sección Reseñas -->
      <div id="section-resenas" class="section d-none">
        <div class="card shadow-sm">
          <div class="card-body">
            <h4 class="mb-3">Tus reseñas</h4>
            <div id="resenasList" class="vstack gap-3"></div>
          </div>
        </div>
      </div>

    </section>
  </main>

  <footer class="text-center py-4">
    <p>&copy; 2025 JESSKA. El equilibrio perfecto entre estilo y funcionalidad</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/perfil.js"></script>
</body>
</html>
